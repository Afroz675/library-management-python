   # 📚 Library Management System

A console-based Python project to manage books, borrowers, and borrowing/returning operations.

## Features
- Add, update, remove **books**
- Add, update, remove **borrowers**
- Borrow and return books with due dates
- Search books by `title`, `author`, or `genre`
- List all books & borrowers

## Requirements
- Python 3.8+
- No external libraries (uses only `datetime`)

## How to run
1. Clone the repo:
   
```bash
   git clone https://github.com/your-username/library-management.git
